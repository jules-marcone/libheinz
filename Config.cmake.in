# - Config file for the libheinz package
# It defines the following variables
#  LibHeinz_INCLUDE_DIRS - include directories for FooBar
#  # LibHeinz_LIBRARIES    - libraries to link against

# Based on https://gitlab.kitware.com/cmake/community/-/wikis/doc/tutorials/How-to-create-a-ProjectConfig.cmake-file

@PACKAGE_INIT@

# Compute paths
get_filename_component(LibHeinz_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(LibHeinz_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# # Our library dependencies (contains definitions for IMPORTED targets)
# if(NOT TARGET foo AND NOT FooBar_BINARY_DIR)
#   include("${LibHeinz_CMAKE_DIR}/FooBarTargets.cmake")
# endif()
#
# # These are IMPORTED targets created by FooBarTargets.cmake
# set(LibHeinz_LIBRARIES foo)
